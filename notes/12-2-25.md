# My updates
- Fixed wrongly ordered compartments
    - getAttributeNames used to form componentMap, a std::unorderedmap, for getting correct compartment indices
- Python visualization script (for more easy use than Paraview)
    - Animations
- Time & error scaling test (`model_test.py`) - automated running of the TestSIR code
- Currently running `./SIR.exe --tf=10 --nt=128 --prefix="NEW"`
    - I think diffusion isn't working
        - Work on the TestSIR code: testing error and visualizing diffusion to see if it's doing ok
            - `./TestSIR.exe --nx=64 --nt=32 --tf=2`

# My questions
- How can I use linear scales with lat-lon coords (w/o coord projection)?
    - In particular: diffusion coefficients are in units of area/time; using lat-lon gives square degrees which do not translate equivalently at different points on Earth, so without doing something to account for this, diffusion will happen differently at different places (when it should not)
- How can I determine realistic test values / ranges for parameters?
    - In particular, the diffusion coefficients

# Moving forward
- It appears that diffusion works for point data (1st order Lagrange interpolants) but not for cells (0th order """)
    - Task: update visualize.py script to allow visualization of point data to confirm that it does work for points
    - Task: run model_test error scaling code for TestSIR MMS with the cell data to see if there is an error scaling difference
- Worked out math for accounting for lat/lon
    - See picture taken and comments in SIR.cpp; how to get sin^2(theta) (i.e. how to reference the coordinates directly in the integrand)?
    - Task: implement this
- Task: determine realistic values/ranges for parameters
