- issue is gibbs phenomenon
    - giving us negative values
- projection theorem, gibbs phenomenon, etc (see OneNote)

- todo: figure out saving timesteps, work on visualization script

- to fix issue:
    - constrained optimization problem
        - replace best fit criterion of projection to have all nonnegative coefficients
    - or: cut off projection at 0  
        - but then you have mass loss, we need our resulting projection to be mass-preserving...
    - s = sum (s_j phi_j), integral (s dOmega) = sum(s_j integral(phi_j dOmega)), s_j >= 0 forall j


2/23 notes:
- Katharine has sent notes on Gibbs phenomenon, as well as on the P0->P1 projection
- Regarding P0->P1
    - M is SPD so the expression to minimize for the quadratic program is convex
    - Constraint 1 restricts sol'n to be on a N1-1 dim hyperplane
    - Need to find good code to solve the constrained quadratic program (would be a PITA to write from scratch)
- Trilinos: Mucho
    - Sundance not built with this, and it is opaque -> not recommended
- LBFGS++? ALGLIB?
- Want to find something based on Eigen (there is a Trilinos interface to Eigen)
    - Needs to support mixtrue of equality and inequality constraint
